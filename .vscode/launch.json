{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch on Chrome",
      "url": "http://localhost:5500/projects",
      "webRoot": "${workspaceFolder}",
    }
  ]
}
